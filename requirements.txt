# Evaluator-only dependencies - exact pinned versions.
# scikit-learn pinned so the committed joblib artifact loads predictably.
# This file is intentionally minimal: it is only for the offline `run.sh`
# evaluator path and excludes FastAPI, XGBoost, Gemini, Playwright, and
# frontend tooling. Use requirements-app.txt for the full product demo/tests.
# Python: 3.11-3.14 (trained artifact); CI runs 3.11, 3.12, 3.13, 3.14.
# Build environment: Python 3.14.4, scikit-learn 1.9.0
pandas==3.0.3
numpy==2.4.6
scikit-learn==1.9.0
scipy==1.17.1
joblib==1.5.3
packaging==24.1
