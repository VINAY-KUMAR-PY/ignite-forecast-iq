# Minimal, evaluator-only dependencies. Every selected version is exact.
# scikit-learn is pinned consistently across Python versions so the committed
# joblib artifact can load predictably in the offline evaluator.
# Python: 3.11-3.14 (trained artifact and evaluator CI)
# Build environment: Python 3.14.4, scikit-learn 1.9.0
pandas==2.3.3; python_version < "3.11"
pandas==3.0.3; python_version >= "3.11"
numpy==2.2.6; python_version < "3.11"
numpy==2.4.6; python_version >= "3.11"
scikit-learn==1.9.0
scipy==1.15.3; python_version < "3.11"
scipy==1.17.1; python_version >= "3.11"
joblib==1.5.3
packaging==24.1
